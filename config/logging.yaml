version: 1

formatters:

  standard:

    format: "%(asctime)s | %(levelname)s | %(message)s"

handlers:

  file:

    class: logging.FileHandler

    filename: logs/project.log

    formatter: standard

root:

  level: INFO

  handlers:
    - file